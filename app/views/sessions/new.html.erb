<div class="row">
  <div class="col s4"></div>
   <div class="col s4 form-block">
      <br>
      <h4 class="center-align">Login</h4>
      <%= form_for @user, url: '/login'  do |form| %>
        <% if @user.errors.any? %>
          <ul class="error-message">
            <% @user.errors.full_messages.each do |error| %>
              <li><%= error %></li>
            <% end %>
          </ul>
        <% end %>

        <div class="row">
          <div class="input-field col s12">
          <i class="material-icons prefix">account_circle</i>
            <%= form.text_field :username, class: 'validate form-field', placeholder: 'Username'%>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s12">
            <i class="material-icons prefix">lock_open</i>
            <%= form.password_field :password, class: 'validate  form-field', placeholder: 'Password' %>
          </div>
        </div>
        <center>
          <%= form.submit 'Login', class: 'btn btn-medium center-align waves-effect waves-light no-uppercase' %>
        </center><br>
      <% end %>
    </div>
    <div class="col s4"></div>
</div>


